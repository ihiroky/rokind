# 通知ウィンドウ不具合の調査と修正記録

## 記録情報

- 記録日：2026-07-24
- 対象リポジトリ：`/home/hiroki/projects/rokind`
- 対象ブランチ：`main`
- 作業時点の HEAD：`2e9c924`
- 作業状態：実装済み、コミット未作成

## 事象

同じ予定の通知が2つ表示されることがある。

そのうち一方は、「閉じる」ボタンを押しても閉じられないことがある。

「Meeting URL を開く」ボタンを押しても、通知が閉じられないことがある。

複数ディスプレイ使用時だけでなく、2台接続中に通知が表示された後、移動や切断によってプライマリディスプレイ1台だけになった場合にも発生する可能性がある。

## 実装から確認した事実

通知ウィンドウは、予定 ID とモニター番号から生成したラベルで管理されていた。

```text
reminder-display-{sanitized_event_id}_{monitor_index}
```

通知生成時は、その時点の `available_monitors()` を列挙して各モニターにウィンドウを作成していた。

通知を閉じる時も `available_monitors()` を再列挙し、その時点のモニター番号に対応するラベルだけを閉じていた。

そのため、通知生成後にディスプレイ構成やモニター列挙順が変わると、生成済みのウィンドウを閉じる側が見つけられない可能性があった。

`window.close()` のエラーは従来無視されていたため、対象ウィンドウを見失った場合と close 操作そのものが失敗した場合を区別できなかった。

会議リンク操作は、`openUrl()` の完了後に dismiss 処理を実行していた。

`openUrl()` が失敗すると、後続の dismiss 処理が実行されない経路があった。

## 原因の評価

最有力の原因は、通知生成時と通知終了時でモニター構成が変わり、古い通知ウィンドウが管理対象から外れることだった。

2台から1台への切り替えは、この条件を満たす具体的な発生経路になる。

起動時から1台でディスプレイ構成も変わっていない場合は、この原因だけでは説明できない。

その場合は、通知生成と close の競合、Tauri の close 失敗、会議リンク起動失敗などをログで切り分ける必要がある。

## 実施した修正

### Rust 側

- モニター番号を使わず、`webview_windows()` から予定 ID の prefix に一致する通知ウィンドウを列挙するよう変更した。
- 予定ごとの通知ウィンドウを閉じる処理を、現在のモニター一覧に依存しないよう変更した。
- 通知の表示更新も、現在のモニター一覧ではなく実在する予定別ウィンドウを対象にするよう変更した。
- 通知ウィンドウの生成、閉鎖、全件閉鎖を Mutex で直列化した。
- dismiss 処理がカレンダー同期処理と競合しないよう、同期処理の Mutex を取得するよう変更した。
- 予定通知の再生成前に、その予定の既存ウィンドウを全て閉じるよう変更した。
- `window.close()` の失敗を debug log に記録し、予定別 close では呼び出し元にもエラーを返すよう変更した。

対象ファイル：[`src-tauri/src/main.rs`](src-tauri/src/main.rs)

### TypeScript 側

- 同じ通知画面で複数回ボタンを押しても、dismiss 処理を重複実行しないようにした。
- 「Meeting URL を開く」では、URL 起動が失敗しても `finally` で dismiss を実行するようにした。
- dismiss 失敗時に debug log を出し、再試行できる状態に戻すようにした。
- 「閉じる」処理と会議リンク処理の失敗を debug log に記録するようにした。

対象ファイル：[`src/main.ts`](src/main.ts)

## 検証結果

- `pnpm run build`：成功
- `cargo fmt -- --check`：成功
- `git diff --check`：成功
- `cargo check`：GTK、WebKit 系のシステムライブラリが環境に無いため完遂できなかった

`cargo check` の失敗は、今回の Rust コードのコンパイルエラーではなく、次のシステムライブラリが見つからないことによる。

```text
pango
gdk-pixbuf-2.0
glib-2.0
libsoup-3.0
javascriptcoregtk-4.1
cairo
```

## 次回再開時に確認すること

1. 実機で2台接続中に通知を表示する。
2. 通知を閉じる前に、片方のディスプレイを切断するかスリープ復帰させる。
3. 1台になった状態で「閉じる」を押し、全ての通知が消えることを確認する。
4. 「Meeting URL を開く」を押し、URL 起動の成功時と失敗時の両方で通知が消えることを確認する。
5. 同じ予定について通知が2つ表示された場合、debug log の `close_event_reminder_windows` に対象ラベルが全て出ているか確認する。
6. 実機で再発した場合は、通知生成時、ディスプレイ構成変更時、dismiss 時のウィンドウラベルを追加記録する。

## 未実施の作業

- 実機での複数ディスプレイ切り替えテスト
- GTK、WebKit 開発パッケージを導入した環境での `cargo check`
- 変更内容の git commit

